// Simple script placeholder. Add interactive behavior here.
// Example: focus the first heading on load.
window.addEventListener('DOMContentLoaded', () => {
  const h1 = document.querySelector('h1');
  if (h1) h1.tabIndex = -1;
});
